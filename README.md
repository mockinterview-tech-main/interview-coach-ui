# Interview Coach UI

## Local Development
1. Add a `.env` file to the root of this project with your keys (see `.env.example`).
2. Run `npm install` and `npm run dev`
3. Set up the [backend](https://github.com/dcaponi/interview-coach-qandaservice)
